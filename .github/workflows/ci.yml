name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm validate:data packages/schemas/test/dataset
      - run: pnpm validate:data
      # Offline scrape over the saved fixtures into a copy of data/ (arch §11 E2E offline).
      - name: E2E offline scrape
        run: |
          cp -R data "$RUNNER_TEMP/data"
          DATA_DIR="$RUNNER_TEMP/data" pnpm scrape --offline --allow-drop --report "$RUNNER_TEMP/reports"
          pnpm validate:data "$RUNNER_TEMP/data/v1"
      - run: pnpm build

  # The built Worker and dist on the real runtime (plan D29): `_headers`, `_redirects`,
  # `run_worker_first`, static ETag/304 and the Worker, which `app.request()` tests never exercise.
  smoke-local:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      WRANGLER_SEND_METRICS: "false"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      # A throwaway key secret, so smoke exercises the key tier and the 401 on a bad key.
      - name: Start wrangler dev
        working-directory: apps/api
        run: |
          echo "API_KEY_SECRET=$(openssl rand -base64 32)" > .dev.vars
          pnpm exec wrangler dev --port 8000 > "$RUNNER_TEMP/wrangler-dev.log" 2>&1 &
          for _ in $(seq 1 60); do
            if curl -sf http://localhost:8000/v1/meta.json > /dev/null; then exit 0; fi
            sleep 1
          done
          cat "$RUNNER_TEMP/wrangler-dev.log"
          exit 1
      # CI builds without `pnpm images:pull` (no B2 key), so images are checked only after deploy.
      - run: pnpm smoke --base http://localhost:8000 --skip-images --wait-seconds 0 --key "$(pnpm --silent key:issue)"
      - if: failure()
        run: cat "$RUNNER_TEMP/wrangler-dev.log"
