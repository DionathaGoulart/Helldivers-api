// Public URLs of the deployment, shared by the Worker (problem `type`s, 429 details) and the build
// (OpenAPI `servers`, docs checks).

export const SITE_URL = "https://helldivers-api.pages.dev";
export const REPO_URL = "https://github.com/DionathaGoulart/Helldivers-api";
/** How to ask for a higher limit (docs page, arch §8.6). */
export const ACCESS_URL = `${SITE_URL}/docs/access`;
